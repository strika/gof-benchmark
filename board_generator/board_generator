#!/usr/bin/env ruby

require "csv"

BOARD_SIZE = 100

def generate_board(size)
  Array.new(size) do
    Array.new(size) { Random.rand(2) }
  end
end

board = generate_board(BOARD_SIZE)

CSV.open("board.csv", "w") do |csv|
  board.each { |row| csv << row }
end
